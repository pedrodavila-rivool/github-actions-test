name: Test Manual Step

on:
  push:

jobs:
  hello:
    runs-on: ubuntu-latest

    steps:
      - name: Echo Hello
        run: echo "hello"

  world:
    needs: hello
    runs-on: ubuntu-latest
    environment: manual-approval

    steps:
      - name: Echo World
        run: echo "world"
